name: ML CD Pipeline

on:
  workflow_run:
    workflows: ["ML CI Pipeline"]   #  link to CI pipeline which we have created
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
    - name: Deploy model
      run: echo "🚀 Model deployed successfully!"
